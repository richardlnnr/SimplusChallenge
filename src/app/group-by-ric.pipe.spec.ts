import { GroupByRicPipe } from './group-by-ric.pipe';

describe('GroupByRicPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupByRicPipe();
    expect(pipe).toBeTruthy();
  });
});
